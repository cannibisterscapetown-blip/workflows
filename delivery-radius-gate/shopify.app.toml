# Generated by `shopify app config link` on a machine authenticated against the
# Partner account. If this file drifts from the app's real settings, re-run:
#
#   shopify app config link --client-id bf8985cecfbe27bbf9dbe30dd4b39ee2
#
# Prefer that over hand-editing — deploying pushes this config to the app, so a
# wrong value here overwrites the real one.

client_id = "bf8985cecfbe27bbf9dbe30dd4b39ee2"
name = "delivery-radius-gate"
application_url = "https://cannibisters.com"
embedded = false

[access_scopes]
# Functions-only app: the function runs inside checkout and needs no Admin API
# scopes of its own.
scopes = ""

[auth]
redirect_urls = [ "https://cannibisters.com/api/auth" ]

[webhooks]
api_version = "2026-07"

[build]
include_config_on_deploy = true
